import type { Client } from "@xdevplatform/xdk";

// XDK: posts.getLikingUsers(id, options)
export async function likers(client: Client, args: string[]): Promise<void> {
  void client, args;
  throw new Error("Not implemented: likers");
}
