import type { Client } from "@xdevplatform/xdk";

// XDK: posts.getQuoted(id, options)
export async function quotes(client: Client, args: string[]): Promise<void> {
  void client, args;
  throw new Error("Not implemented: quotes");
}
