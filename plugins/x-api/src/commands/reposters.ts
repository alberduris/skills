import type { Client } from "@xdevplatform/xdk";

// XDK: posts.getRepostedBy(id, options)
export async function reposters(client: Client, args: string[]): Promise<void> {
  void client, args;
  throw new Error("Not implemented: reposters");
}
