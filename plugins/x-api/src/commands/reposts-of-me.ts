import type { Client } from "@xdevplatform/xdk";

// XDK: users.getRepostsOfMe(options)
export async function repostsOfMe(client: Client, args: string[]): Promise<void> {
  void client, args;
  throw new Error("Not implemented: reposts-of-me");
}
