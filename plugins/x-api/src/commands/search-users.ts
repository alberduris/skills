import type { Client } from "@xdevplatform/xdk";

// XDK: users.search(query, options)
export async function searchUsers(client: Client, args: string[]): Promise<void> {
  void client, args;
  throw new Error("Not implemented: search-users");
}
